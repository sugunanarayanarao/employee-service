[build-system]
requires = ["setuptools>=65", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "employee-service"
version = "1.0.0"
description = "Employee REST API"
dependencies = [
    "fastapi",
    "uvicorn"
]

[tool.setuptools.packages.find]
include = ["employee_service*"]
